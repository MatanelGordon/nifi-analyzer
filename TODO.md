# TODO

- Decouple console logs from implementation. put events there instead.
- make a socket io that streams log messages to the client to show what is being processed. 
- Improve error handling for Server
- Fix Foreign Key Constraint
- Fix Busy db file when error
- Add explanation on max provenanace - that is for each query
- Move llm button to the corner
- Add explanation about the project in general
- Make copy button to select the entire text for a manual copy if needed.
- Add button for "Separate Prompt Message" with text saying what it will do.
- Add to llm.md an example for time bucketing
- Add to llm.md an example for average for nodes in node_status_history.
- Add to llm.md tip to find performance using lineage duration gaps and making a query that finds the largest avarage lineage gaps between 2 connected Processors.
